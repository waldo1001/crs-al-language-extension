import { join } from 'path';
import { POWERSHELLPATH } from './PSScripts';

export const MODULESPATH                   = join(POWERSHELLPATH, 'modules');

//Any common PS functions or custom PS modules can be added here